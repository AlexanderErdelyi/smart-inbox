# Database Bootstrap

Place migrations and seed scripts in this directory.

Suggested next steps:
- Choose ORM and migration tool (for example Prisma).
- Add initial schema for users, mailbox_accounts, messages, threads, labels, and sync_cursors.
